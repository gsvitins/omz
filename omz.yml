---
- hosts: localhost
  roles:
    - role: '{{playbook_dir}}'
  vars:
    omz_plugins: "colored-man-pages docker docker-compose git git-prompt history history-substring-search kubectl helm nmap pip python rsync systemd ubuntu vagrant vi-mode aws terraform zsh-autosuggestions"
    omz_users:
      - bxz
